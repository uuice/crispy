---
description: Admin AI（字段 AI + 对话 Agent）开发约定
globs: src/ai/**/*.ts,src/components/AdminAi*/**,src/app/(payload)/api/ai/**
alwaysApply: false
---

# AI & Agent

## 两套 AI

| 类型 | 入口 | 用途 |
|------|------|------|
| 字段 AI | `AdminAi/*`、`/api/ai/stream\|complete\|structured` | 润色、SEO、Lexical 选区 |
| 对话 Agent | `/admin/ai-agent`、`/api/ai/agent` | Function Calling CRUD |

## Agent 约定

- 可管理范围: `src/ai/agent/resources.ts`（与 MCP 大致对齐）
- 写操作前: 工具 `describe_resource` 了解字段结构
- 删除: `delete_document` → `trashOrDeleteDocument`（软删除，media 不可删）
- `app-configs` 写操作: 仅 super-admin
- author: 仅自己的 posts

## 扩展 Agent 工具

1. 在 `src/ai/agent/tools.ts` 增加定义 + handler
2. 更新 `systemPrompt.ts` 说明
3. 检查 `src/ai/agent/access.ts` 权限

## 扩展字段 AI

1. `src/ai/collectionProfiles.ts` 增加 profile
2. 字段用 `withAiTextField` 等 wrapper
3. `pnpm generate:importmap`

## 环境

- `DEEPSEEK_API_KEY` + Admin → AI 设置 Global
- 语义搜索需 Postgres pgvector（`src/ai/embeddings/`）
