---
description: Admin AI Agent 与前台只读助手开发约定
globs: src/ai/**/*.ts,src/components/AdminAiAgent/**,src/components/FrontendAiAssistant/**,src/app/(payload)/api/ai/**,src/app/(frontend)/api/ai/**
alwaysApply: false
---

# AI & Agent

## 两套 AI

| 类型 | 入口 | 用途 |
|------|------|------|
| 后台 Agent | `/admin/ai-agent`、`/api/ai/agent` | Function Calling CRUD（需登录） |
| 前台助手 | `FrontendAiAssistant`、`/api/ai/assistant` | 公开只读检索（无需登录） |

## 后台 Agent 约定

- 可管理范围: `src/ai/agent/resources.ts`（与 MCP 大致对齐）
- 写操作前: 工具 `describe_resource` 了解字段结构
- 删除: `delete_document` → `trashOrDeleteDocument`（软删除，media 不可删）
- 鉴权: `src/ai/agent/access.ts` 按 Permission（与 Admin `can()` 对齐）；`app-configs` 写需 `catalog:app-configs:write`
- 无 `posts:update:any` 时仅能管理自己的 posts

## 扩展后台 Agent 工具

1. 在 `src/ai/agent/tools.ts` 增加定义 + handler
2. 更新 `systemPrompt.ts` 说明
3. 检查 `src/ai/agent/access.ts` 权限

## 扩展前台助手工具

1. 在 `src/ai/frontend-assistant/tools.ts` 增加定义 + handler
2. 更新 `systemPrompt.ts` 与 `publicContent.ts`（仅公开可读数据）
3. 勿复用后台 Agent 写操作工具；查询须 `overrideAccess: false`

## 配置

- Admin 配置中心：`llm-providers` + `ai-settings`（见 `docs/dev-docs.md#config-center`）
- Prompt 模板：`prompt-templates`（Agent 可维护；`resolveLlmClient` 可按 action / promptId 解析）
- 语义搜索需 Postgres pgvector（`src/ai/embeddings/`）+ Embedding Provider（capabilities=embedding）
