---
description: Admin AI（字段 AI + 对话 Agent）与前台只读助手开发约定
globs: src/ai/**/*.ts,src/components/AdminAi*/**,src/components/FrontendAiAssistant/**,src/app/(payload)/api/ai/**,src/app/(frontend)/api/ai/**
alwaysApply: false
---

# AI & Agent

## 三套 AI

| 类型 | 入口 | 用途 |
|------|------|------|
| 字段 AI | `AdminAi/*`、`/api/ai/stream\|complete\|structured` | 润色、SEO、Lexical 选区（Admin） |
| 后台 Agent | `/admin/ai-agent`、`/api/ai/agent` | Function Calling CRUD（需登录） |
| 前台助手 | `FrontendAiAssistant`、`/api/ai/assistant` | 公开只读检索（无需登录） |

## 后台 Agent 约定

- 可管理范围: `src/ai/agent/resources.ts`（与 MCP 大致对齐）
- 写操作前: 工具 `describe_resource` 了解字段结构
- 删除: `delete_document` → `trashOrDeleteDocument`（软删除，media 不可删）
- `app-configs` 写操作: 仅 super-admin
- author: 仅自己的 posts

## 扩展后台 Agent 工具

1. 在 `src/ai/agent/tools.ts` 增加定义 + handler
2. 更新 `systemPrompt.ts` 说明
3. 检查 `src/ai/agent/access.ts` 权限

## 扩展前台助手工具

1. 在 `src/ai/frontend-assistant/tools.ts` 增加定义 + handler
2. 更新 `systemPrompt.ts` 与 `publicContent.ts`（仅公开可读数据）
3. 勿复用后台 Agent 写操作工具；查询须 `overrideAccess: false`

## 扩展字段 AI

1. `src/ai/collectionProfiles.ts` 增加 profile
2. 字段用 `withAiTextField` 等 wrapper
3. `pnpm generate:importmap`

## 环境

- `DEEPSEEK_API_KEY` + Admin → AI 设置 Global
- 语义搜索需 Postgres pgvector（`src/ai/embeddings/`）
