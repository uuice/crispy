/** Default ISR / data cache seconds (keep in sync with CacheSettings global defaults). */
export const DEFAULT_PAGE_REVALIDATE = 600
export const DEFAULT_DATA_CACHE_REVALIDATE = 600

/** Used by frontend route segments (`export const revalidate`). */
export const PAGE_REVALIDATE_SECONDS = DEFAULT_PAGE_REVALIDATE
